export const altoContrasteAtivo = (altoContraste: boolean) => altoContraste;

export const aplicarTamanhoFonte = (tamanhoFonte: number) => {
  document.documentElement.style.fontSize = `${tamanhoFonte}%`;
};
